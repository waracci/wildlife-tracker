<h3>Add Animal</h3>
<form action="/animals/new" method="post">
  <div class="form-group">
    <label for="name">Name the animal: </label>
    <input type="text" name="name" class="form-control">
  </div>
  <div class="form-group">
    <label for="species">Species: </label>
    <input type="text" name="species" class="form-control">
  </div>
  <div class="checkbox">
    <label for="species">
    <input type="checkbox" name="endangered"> Endangered?
    </label>
  </div>
  <div class="form-group">
    <label for="health">Health (as far you can tell)</label>
    <select class="form-control reveal-if-active" name="health">
      <option>$endangered.HEALTH_HEALTHY</option>
      <option>$endangered.HEALTH_OK</option>
      <option>$endangered.HEALTH_ILL</option>
    </select>
  </div>
  <div class="form-group">
    <label for="age">Age (as far you can tell)</label>
    <select class="form-control reveal-if-active" name="age">
      <option>$endangered.AGE_BABBY</option>
      <option>$endangered.AGE_YOUNG</option>
      <option>$endangered.AGE_ADULT</option>
    </select>
  </div>
  <button type="submit" name="button">Add Animal</button>
</form>
